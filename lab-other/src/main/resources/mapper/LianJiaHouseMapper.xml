<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.yiidii.lab.other.mapper.LianJiaHouseMapper">
    <resultMap type="cn.yiidii.lab.other.model.entity.LianJiaHouse" id="LianJiaHouse">
        <id property="id" column="id"/>
        <result property="houseCode" column="house_code"/>
        <result property="picture" column="picture"/>
        <result property="title" column="title"/>
        <result property="price" column="price"/>
        <result property="provinceId" column="province_id"/>
        <result property="cityId" column="city_id"/>
        <result property="districtId" column="district_id"/>
        <result property="address" column="address"/>
        <result property="area" column="area"/>
        <result property="orientation" column="orientation"/>
        <result property="type" column="type"/>
        <result property="tags" column="tags"/>
        <result property="lastMaintainTime" column="last_maintain_time"/>
        <result property="date" column="date"/>
        <result property="createTime" column="create_time"/>
        <result property="createdBy" column="create_by"/>
    </resultMap>

    <!-- Mapper -->
    <insert id="batchSaveOrUpdateOnDuplicateUpdate" parameterType="java.util.List">
        insert into lianjia_house(`id`,`house_code`, `picture`, `title`, `price`, `province_id`, `city_id`, `district_id`, `address`, `area`, `orientation`, `type`, `tags`, `last_maintain_time`, `date`,`create_time`) values
        <foreach collection="list" item="item" separator=",">
            (
            #{item.id},
            #{item.houseCode},
            #{item.picture},
            #{item.title},
            #{item.price},
            #{item.provinceId},
            #{item.cityId},
            #{item.districtId},
            #{item.address},
            #{item.area},
            #{item.orientation},
            #{item.type},
            #{item.tags},
            #{item.lastMaintainTime},
            #{item.date},
            #{item.createTime}
            )
        </foreach>
        on DUPLICATE KEY UPDATE
        picture=values(picture),
        title=values(title),
        price=values(price),
        province_id=values(province_id),
        city_id=values(city_id),
        district_id=values(district_id),
        address=values(address),
        area=values(area),
        orientation=values(orientation),
        type=values(type),
        tags=values(tags),
        date=values(date)
    </insert>

    <select id="selectListPage" resultType="cn.yiidii.lab.other.model.entity.LianJiaHouse">
        SELECT lh.*
        FROM (SELECT house_code, max(id) id FROM lianjia_house GROUP BY house_code) uni
                 LEFT JOIN lianjia_house lh ON uni.id = lh.id
    </select>

</mapper>
